# Governance Tool Archive

This namespace contains legacy governance wrappers that are no longer part of
the current top-level Airivo operator surface.

The current slimming direction is to make daily evidence gates explicit under
Top5/v9 operations. Historical governance gate references remain available for
forensic review, but should not force archived one-off tools back into top-level
`tools/`.
