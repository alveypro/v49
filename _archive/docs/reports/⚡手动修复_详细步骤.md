# ⚡ 手动修复详细步骤

## 问题现状
- 当前文件：约9892行
- 需要保留：前6678行
- 需要删除：6679-9892行（约3214行旧代码）

---

## 🎯 方法1：Python脚本（最简单）

在终端执行：
```bash
cd /Users/mac/QLIB
python SIMPLE_FIX.py
```

**说明**：这个脚本会自动完成所有工作，包括备份和删除。

---

## 🎯 方法2：VSCode/Cursor手动删除

### 步骤1：定位行号
1. 按 `Cmd+P`（Mac）或 `Ctrl+P`（Windows）
2. 输入 `:6678` 回车（跳转到6678行）

### 步骤2：确认位置
你应该看到这两行：
```python
if __name__ == "__main__":
    main()
```

### 步骤3：选择要删除的内容
1. 光标定位在第6678行末尾（`main()` 这行）
2. 按 `Cmd+Shift+End`（Mac）或 `Ctrl+Shift+End`（Windows）
   - 这会选中从当前位置到文件末尾的所有内容
3. 确认选中了约3214行

### 步骤4：删除
1. 按 `Delete` 或 `Backspace`
2. 文件应该现在只有6678行

### 步骤5：保存
1. 按 `Cmd+S`（Mac）或 `Ctrl+S`（Windows）

---

## 🎯 方法3：使用sed命令

在终端执行：
```bash
cd /Users/mac/QLIB

# 备份原文件
cp 终极量价暴涨系统_v49.0_长期稳健版.py 终极量价暴涨系统_v49.0_长期稳健版.py.backup

# 只保留前6678行
sed -n '1,6678p' 终极量价暴涨系统_v49.0_长期稳健版.py.backup > 终极量价暴涨系统_v49.0_长期稳健版.py

# 验证行数
wc -l 终极量价暴涨系统_v49.0_长期稳健版.py
```

应该显示：`6678 终极量价暴涨系统_v49.0_长期稳健版.py`

---

## ✅ 验证修复

修复完成后，检查文件行数：
```bash
wc -l /Users/mac/QLIB/终极量价暴涨系统_v49.0_长期稳健版.py
```

**正确结果**：应该显示 `6678`

---

## 🚀 测试运行

```bash
cd /Users/mac/QLIB
streamlit run 终极量价暴涨系统_v49.0_长期稳健版.py
```

进入"📊 超级回测系统"Tab，应该只看到回测相关内容。

---

## 🆘 如果出现问题

### 问题1：执行sed报错
**解决**：使用方法1（Python脚本）或方法2（手动删除）

### 问题2：修复后系统报错
**解决**：恢复备份
```bash
cp 终极量价暴涨系统_v49.0_长期稳健版.py.backup 终极量价暴涨系统_v49.0_长期稳健版.py
```

### 问题3：不知道如何使用终端
**解决**：
1. Mac：打开"应用程序" → "实用工具" → "终端"
2. Windows：按 `Win+R`，输入 `cmd`，回车

---

**推荐方案**：方法1（Python脚本）最简单可靠！

```bash
cd /Users/mac/QLIB && python SIMPLE_FIX.py
```

